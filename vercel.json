{
  "framework": "nextjs",
  "buildCommand": "if [ -f package.json ]; then npm run build; else npm --prefix frontend run build; fi",
  "installCommand": "if [ -f package.json ]; then npm install; else npm --prefix frontend install; fi",
  "env": {
    "NEXT_PUBLIC_API_BASE_URL": "https://hydrosentinel-api.onrender.com/api/v1"
  }
}